# TODO

## 工程卫生

- [x] 依赖不可复现：无 requirements.txt / pyproject.toml。
      （2026-08-26 已解决：pyproject.toml + src-layout 打包为 bfGBZ2d，
      运行时依赖仅 numpy+scipy；见 log/2026-08-26-bfgbz2d-packaging.md）
- [ ] 无 CI、无 lint 配置（ruff/pyflakes/mypy 均未装）。
      （打包后已可配 GitHub Actions：pip install -e .[dev] + pytest，
      建议加 POLY_BACKEND=numpy 矩阵腿）

## Amoeba

- [ ] amoeba：`mu2_mid` 配对 `w_left` 的 ε 级偏移、Newton clamp 到
      初始括区间。

